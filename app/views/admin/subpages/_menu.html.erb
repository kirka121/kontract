<% @subpages.each do |page| %>
	<%= page.title %>
	<%= page.content %>
	<%= page.user %>
	<%= link_to "Edit" ,edit_admin_subpage_path(page) %>
	<%= link_to 'Destroy',  admin_subpage_path(page),  method: :delete, data: { confirm: 'Are you sure?' } %>
	</br>
<% end %>		