tinymce.remove()
$('.tinymce').each(function(elem) {tinyMCE.execCommand('mceAddControl',false,elem.id);});

$("#subsection-modal").html("<%= escape_javascript(render 'new') %>");
$("#subsection-modal").hide().fadeIn("slow");
