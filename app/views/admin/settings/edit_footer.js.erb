$("#mainsettings-modal").html("<%= escape_javascript(render 'edit_footer') %>");
$("#mainsettings-modal").hide().fadeIn("slow");

$(".footermode").click(function() {
	$(".footermode").addClass("active").not(this).removeClass("active");
	$("#setting_footer_mode").attr("value", this.value);
});