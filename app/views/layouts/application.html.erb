<!DOCTYPE html>
<html>
<head>
	<title><%= yield_site_settings.site_title %></title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= favicon_link_tag 'favicon.ico' %>
	<%= csrf_meta_tags %>
	<meta charset='utf-8'>
	<meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
	<!-- TODO: \/ figure out a better way to filuter out control panel yield vs visitor / client yield -->
	<% if controller_name=='admin' || params[:controller]=='admin/subpages' || params[:controller]=='admin/settings' || params[:controller] == 'admin/teams' || params[:controller] == 'admin/services'%>
		<%= render 'admin/partials/tophalf' %>
		<div id="main_site_flashes">
			<%= render 'layouts/partials/flashes' %>
		</div>
			<%= yield %>
		<%= render 'admin/partials/bottomhalf' %>
	<% elsif controller_name=='sessions' && action_name=='new'  %>
		<div id="main_site_flashes">
			<%= render 'layouts/partials/flashes' %>
		</div>
		<%= yield %>
	<% else %>
		<%= render 'layouts/partials/header' %>
		<div id="main_contrainer">
			<div id="main_site_flashes">
				<%= render 'layouts/partials/flashes' %>
			</div>
			<%= yield %>
		</div>
		<%= render 'layouts/partials/footer' %>
	<% end %>
</body>
</html>
